[package]
name = "ue-asset"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1"
# CityHash64: package ids, export hashes and script-import indices all derive from it.
ue-iostore = { path = "../ue-iostore" }
